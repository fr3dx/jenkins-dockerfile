pipeline {
    agent { dockerfile true }
    stages {        
        stage ('Build Docker image'){
            steps{
                echo 'Image done'
            }
            
        }
        
    }
}
